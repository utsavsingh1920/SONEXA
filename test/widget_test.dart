import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SONEXA app test', (WidgetTester tester) async {
    // SONEXA widget test can be added here later.
    expect(true, isTrue);
  });
}

